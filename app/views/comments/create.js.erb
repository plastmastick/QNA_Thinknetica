<% if @commentable.is_a?(Answer) %>
  $("#answer-<%= @commentable.id %> .create-comment-errors").html('<%= j render 'shared/errors', resource: @comment %>');
<% elsif @commentable.is_a?(Question) %>
  $('.question-errors').html('<%= j render 'shared/errors', resource: @comment %>');
<% end %>
